# Ingest + parser (dev/local)
datasets>=2.20
huggingface_hub>=0.24
boto3>=1.34
pydantic>=2.6

# Test + lint
pytest>=8.0
ruff>=0.6

# dbt. Pinned to a compatible core/adapter pair — mismatches between
# dbt-core and the adapter are a classic hour-waster.
#
# NOTE: the project plan says `dbt-athena-community`. That package is now
# only a backwards-compatibility wrapper around `dbt-athena`, which dbt
# Labs took over maintaining in late 2024. Installing `dbt-athena`
# directly is the current guidance; installing both risks a conflict.
dbt-core>=1.8,<2.0
dbt-athena>=1.8,<2.0
